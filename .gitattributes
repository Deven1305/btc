# Force LF (Unix) line endings for shell scripts and python scripts
*.sh text eol=lf
*.py text eol=lf
*.parquet binary
*.csv text
*.md text

# The exhaustive Kaggle CSV is above GitHub's ordinary-file size limit.
queues/kaggle_all_addresses.csv filter=lfs diff=lfs merge=lfs -text
